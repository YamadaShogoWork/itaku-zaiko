# 2026-06-27 作業ログ

## 実施内容

### 仕様確認（実装前）
- タスク各件の仕様を確認・決定
  - #1 Remember me: 有効期間60日
  - #3 掛け率スピナー: min/max なし
  - #6 納品履歴の現在庫列: 対応しない
  - #13 期末在庫必須チェック: 保存時のみ、条件は「期首在庫 > 0」
  - #14 下代・売上額の小数: 画面・Excel両方で切り捨て表示（後で変更可能性あり）
  - #15 同名警告: 商品は不要。取引先・色はフォーカスアウト時に警告
  - #16 確認ダイアログ: モックアップ3パターン作成→パターンC（センター大アイコン）を採用

### タスク #16：確認ダイアログのカスタムデザイン実装
- `docs/mockups/dialog.html` 作成（A/B/C 3パターンを比較できるインタラクティブなモックアップ）
- `wwwroot/css/common.css` にダイアログスタイル追加（オーバーレイ・アニメーション・アイコン色分け5種）
- `wwwroot/js/site.js` に `Dialog` オブジェクト実装
  - `Dialog.confirm({ title, message, confirmText, cancelText, type, onConfirm, onCancel })`
  - `Dialog.alert({ title, message, type, closeText, onClose })`
  - `data-confirm-title` を持つフォームの送信をインターセプトする共通ハンドラ
  - `data-confirm-form` を持つボタンのクリックをインターセプトする共通ハンドラ
- `Views/Shared/_Layout.cshtml` にダイアログHTML追加・`site.js` 読み込み追加
- 既存の `confirm()` / `alert()` をビュー14箇所で `Dialog.*` に置き換え
  - フォームの `onsubmit="return confirm(...)"` → `data-confirm-*` 属性に変換（6画面）
  - `onclick` / スクリプトブロック内の `confirm()` / `alert()` → Dialog API に変換（8箇所）

### タスク #5：ドラッグ＆ドロップ並び替え（DBスキーマ変更あり）
- `Models/ClientProduct.cs` に `SortOrder` カラム追加（マイグレーション適用）
- `ViewModels/ClientProductRowViewModel.cs` に `SortOrder` 追加
- `Controllers/ClientController.cs`: `BuildProductRows` で SortOrder 順に並べ替え、保存時に `SortOrder` を反映
- `Controllers/DeliveryController.cs`, `SalesReportController.cs`: ClientProduct の並び順を SortOrder 基準に変更
- `Views/Client/Edit.cshtml`: SortableJS 導入、チェック済み商品を `<tbody>` 単位でドラッグ可能に

### タスク残件（18タスク）一括実装

**#20: Razorバグ修正**
- `Client/Index.cshtml`, `Color/Index.cshtml`: `全@Model.Count件` → `全@(Model.Count)件`

**#2: 掛け率デフォルト値**
- `ProductEditViewModel.cs`: `CommissionRate = 0.8m` をデフォルト値として設定

**#3: 掛け率スピナー**
- `ProductEditViewModel.cs`: `[Range(0.0, 1.0)]` バリデーション削除（上限下限なし）
- `Views/Product/Edit.cshtml`: `step="0.1"` に変更、`min`/`max` 属性削除

**#9: 対象年月デフォルト**
- `SalesReportController.cs`: `GetDefaultYearMonth()` を追加。15日以前→先月、16日以降→今月

**#10: 数値列ヘッダー右寄せ**
- `Views/SalesReport/Index.cshtml`, `Views/SalesReportHistory/Index.cshtml`: `th.num { text-align:right }` 追加

**#11: 期末在庫初期値を空欄**
- `Views/SalesReport/Index.cshtml`: 期末在庫 input から `value="@row.ClosingStock"` を削除

**#12: 数値列の固定幅**
- `Views/SalesReport/Index.cshtml`: `td.num, th.num { white-space:nowrap; min-width:4.5em }` 追加

**#13: 期末在庫バリデーション**
- 既存実装で対応済み（Controller: 期首在庫>0 の場合のみ必須、保存時のみ）

**#14: 下代・売上額の切り捨て表示**
- `Views/SalesReport/Index.cshtml`: `Math.Floor` 適用（Razor + JS `recalcAll`）
- `Views/SalesReportHistory/Index.cshtml`: `Math.Floor` 適用
- `Views/SalesReportHistory/_PreviewContent.cshtml`: 行・合計に `Math.Floor` 適用
- `Views/Client/Edit.cshtml`: 下代表示と JS を `Math.floor` に変更
- `Services/ExcelOutputService.cs`: 下代・売上額・合計に `Math.Floor` 適用

**#15: 同名警告（フォーカスアウト時）**
- `ClientController.cs`, `ColorController.cs`: `CheckDuplicate` GET エンドポイント追加
- `Views/Client/Edit.cshtml`, `Views/Color/Index.cshtml`: blur イベントで AJAX 呼び出し・インライン警告表示

**#17: 検索条件クリアボタン**
- `Views/DeliveryHistory/Index.cshtml`, `Views/SalesReportHistory/Index.cshtml`: 「クリア」ボタン追加

**#18: 検索条件空の場合は全件表示**
- `DeliveryHistoryController.cs`, `SalesReportHistoryController.cs`: `HasSearch` 廃止、常にクエリ実行
- 条件指定時のみフィルタ、なければ全件表示

**#19: ページング（100件）**
- `DeliveryHistoryViewModel.cs`, `SalesReportHistoryViewModel.cs`: `Page`, `TotalCount`, `HasPrev`, `HasNext`, `PageSize=100` 追加
- 各コントローラー: `page` パラメータ追加、Skip/Take 適用
- 各ビュー: 「前へ/次へ」ページング UI 追加

**#7: 納品履歴に帳票ボタン**
- `Views/DeliveryHistory/Index.cshtml`: `IsCarryOver=false` の行に帳票ボタン追加

**#8: 納品登録 カードレイアウト**
- `Views/Delivery/Index.cshtml`: search-bar の `margin-bottom` と `min-width` を削除して納品履歴と統一

**#1: Remember me チェックボックス**
- `Program.cs`: `ConfigureApplicationCookie` で60日有効・SlidingExpiration 設定
- `Areas/Identity/Pages/Account/Login.cshtml.cs`: `InputModel.RememberMe` プロパティ追加
- `Areas/Identity/Pages/Account/Login.cshtml`: チェックボックス追加

**#21: テストデータ投入バッチ**
- `Controllers/SeedDataController.cs` 新規作成（開発環境のみ `/SeedData` でアクセス可）
- A（最小）/ B（標準）/ C（大量）の3パターン対応

### ドキュメント同期
- `docs/handover_memo.md`: ステータスを「全タスク実装完了・手動テスト待ち」に更新、次のステップをテスト実施のみに整理
- `docs/manual_test.md`: 新機能のテストケースを追加
  - セクション0: テストデータ投入バッチ（`/SeedData`）
  - 1-4〜1-5: Remember me チェックボックス
  - 2-3〜2-4: 色の同名重複警告（フォーカスアウト時）
  - 4-4〜4-5: 取引先の同名重複警告
  - 7-1〜7-8: 全件表示・クリアボタン・帳票リンク・ページング（納品履歴）
  - 9-1〜9-2, 9-11: 全件表示・クリアボタン・ページング（売上報告履歴）

## 次回以降
- `docs/manual_test.md` に従って手動テストを実施する
