# 2026-06-22

## 仕様確認・設計書更新

- 未確定事項を全件解決し、各 screen_specs に反映
  - `ApplicationUser.CreatedAt` → 追加する
  - ダッシュボードの売上合計対象年月 → 直近登録済みの `SalesReport.YearMonth`（月末登録のため現在月固定では空になるため）
  - FAX番号 → 任意（NULL許可）
  - 商品編集の掛け率変更 → 既存 `ClientProduct.CommissionRate` への反映有無を確認するダイアログを新設（`product-edit.md` 6.0章）
  - 商品削除 → 削除ブロック方針（`IsActive` フラグなし）で確定
  - Excelファイル名規則 → `委託販売納品書_{取引先名}_{YYYYMM}.xlsx`（3タイミング共通）
  - 検索条件「取引先」デフォルト → 未選択（空欄）

- セキュリティ・性能レビューを実施し、以下を設計書に追記
  - `requirements.md` 5章: `SalesReport`・`ClientProduct` のユニーク制約、`Delivery` の複合インデックスを追記
  - `delivery.md` 6章: 同月の納品日が既に4件ある場合、新規納品日での登録をエラーにする仕様を追記

## 実装着手（プロジェクト作成・DB設計）

- `dotnet new mvc -o Zaiko --auth Individual` でプロジェクト作成（.NET 10、ASP.NET Core Identity + SQLite）
- ClosedXML パッケージを追加（Excel出力用）
- Model クラスを新規作成
  - `AuditableEntity`（基底クラス: CreatedAt/CreatedBy/UpdatedAt/UpdatedBy）
  - `ApplicationUser : IdentityUser`（CreatedAt 追加）
  - `Client`、`Color`、`Size`、`Product`、`Delivery`、`SalesReport`、`ClientProduct`
  - `Delivery.DeliveredAt` は `DateOnly` 型（日付のみ）
- `ApplicationDbContext` を更新
  - `IdentityDbContext<ApplicationUser>` に変更
  - 全エンティティの `DbSet` を追加
  - `OnModelCreating` でユニーク制約・複合インデックスを定義
- `Program.cs` を更新
  - `RequireConfirmedAccount = false`（メール確認なし）
  - グローバル認証必須ポリシー（`SetFallbackPolicy`）を設定
- 初回マイグレーション `InitialCreate` を作成・適用（DB作成完了）

## 実装（Controller・View・Services 全フェーズ完了）

### フェーズ0: 共通基盤
- `wwwroot/css/common.css`、`Views/Shared/_Layout.cshtml`（サイドバーレイアウト）整備
- `ApplicationDbContext.SaveChangesAsync` オーバーライドで監査カラム自動設定（`IHttpContextAccessor` 使用）
- TempData["Success"]/["Error"] によるトースト通知を `_Layout.cshtml` に実装（3秒で自動消去）

### フェーズ1: マスタ管理
- **Client**: `ClientController`・`Views/Client/Index.cshtml`・`Views/Client/Edit.cshtml`
  - 取扱商品チェックボックス一覧（ProductName でグルーピング・rowspan表示）
  - 掛け率入力（`ClientProduct.CommissionRate`）、下代リアルタイム表示（JS）
  - 削除: 関連データあり→`IsActive=false`、なし→物理削除
- **Color**: `ColorController`・`Views/Color/Index.cshtml`（インライン編集、使用中削除ブロック）
- **Product**: `ProductController`・`Views/Product/Index.cshtml`・`Views/Product/Edit.cshtml`
  - 色あり/なしトグル、色チェックボックスグリッド（使用中は削除不可・hidden送信）
  - 掛け率変更時に `ClientProduct.CommissionRate` 一括更新の確認カスタムモーダル

### フェーズ2: ユーザー管理
- `UsersController`・`Views/Users/Index.cshtml`・`Views/Users/Edit.cshtml`
- 自分自身・最後の1人は削除不可（サーバー・クライアント両側チェック）

### フェーズ3: 共通サービス
- `Services/StockCalculationService.cs`: 現在庫算出（バッチ一括取得でN+1回避）
- `Services/ExcelOutputService.cs`: ClosedXML で委託販売納品書Excel生成
- `Services/ExcelReportModels.cs`: Excel帳票データモデル
- `Services/SalesReportService.cs`: `BuildExcelDataAsync` を抽出（SalesReport/SalesReportHistoryで共用）

### フェーズ4: 納品管理
- `DeliveryController`・`Views/Delivery/Index.cshtml`
  - 商品グリッド（ProductName rowspan）、数量入力、合計欄、現在庫表示
  - 4納品日制限チェック、TempData → Redirect → JS自動ダウンロード
- `DeliveryHistoryController`・`Views/DeliveryHistory/Index.cshtml`
  - グループ単位（取引先×納品日×IsCarryOver）での一覧・削除

### フェーズ5: 売上報告管理
- `SalesReportController`・`Views/SalesReport/Index.cshtml`
  - 複数納品日列（最大4列）、期末在庫入力、売上点数・売上額リアルタイム計算（JS）
  - 翌月SalesReport存在時: 読み取り専用モード（入力無効化・保存ボタン非表示・バナー表示）
  - 保存時: SalesReportのUPSERT + 翌月繰越Delivery削除・再作成 + Excel自動ダウンロード
- `SalesReportHistoryController`・`Views/SalesReportHistory/Index.cshtml`・`_PreviewContent.cshtml`
  - AJAX帳票プレビューモーダル、再出力Excel、削除（翌月分存在チェック）

### フェーズ6: ダッシュボード
- `DashboardController`・`ViewModels/DashboardViewModel.cs`・`Views/Dashboard/Index.cshtml`
  - サマリーカード（取引先数・今月納品件数・売上合計・在庫アラート件数）
  - 取引先別売上ランキング（上位4件＋その他、バーチャート）
  - 在庫アラート一覧（危険1-2点:赤、警告3-5点:橙）
  - 最近の納品履歴テーブル
- `Program.cs`: デフォルトルートを `Dashboard` に変更
